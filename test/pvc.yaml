apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs
  namespace: atom-test
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: example-nfs
  resources:
    requests:
      storage: 1Gi
